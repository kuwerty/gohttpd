Starts an HTTP server and dishes out whatever directory you started in or specify on the command line. Serves on port 8000 or whatever you specify on the command line.

You could write this in 20 seconds flat. We get it. Now you don't have to.
